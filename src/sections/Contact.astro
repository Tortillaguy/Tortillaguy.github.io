<div class="mt-14">
  <style>
    .gradient-button {
      --background: #262626;
      background: linear-gradient(var(--background), var(--background))
          padding-box,
        linear-gradient(to right, #e587ff, #1451ec) border-box;
      border-radius: 40px;
      border: 4px solid transparent;
    }
    /* .gradient-button:active {
      --background: transparent;
      background: linear-gradient(var(--background), var(--background))
          padding-box,
        linear-gradient(to right, darkblue, darkorchid) border-box;
      border-radius: 8px;
      border: 4px solid transparent;
    } */
  </style>
  <div class="w-full flex justify-center m-auto prose">
    <button
      class="text-white text-shadow text-3xl gradient-button transition-transform active:scale-110 p-6"
    >
      Let's Get In Touch 🤝
    </button>
    <div class="flex"></div>
  </div>
</div>
